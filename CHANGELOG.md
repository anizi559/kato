# Changelog

## v1.0.0（2026-08-07）— 第一个大版本

### 架构

- AnyTLS 单端口多用户：每个入站一个端口服务所有用户，移除用户级端口分配（20000+）。
- 中转规则固定端口，不再按用户展开。
- 按用户流量统计迁移到 sing-box V2Ray API（`user>>>userId>>>traffic>>>uplink/downlink`）。
- 按用户限速：每用户 `bandwidth-limiter` + `auth_user` 路由规则。
- 权限组新增“超额策略”：断流 / 限速 1Mbps。
- 用户用量不再参与配置 ETag，只有跨流量阈值或管理变更才触发节点配置更新。
- 中转流量只做聚合统计，用户归属只在节点侧完成，避免重复计数。

### 前端

- 权限组节点选择器改为圆角矩形 Chip（自适应换行、整块点击选中变蓝）。
- 用户详情新增订阅二维码，重置订阅令牌后自动更新。
- 修复隐藏路径部署白屏（Vite `base: "./"`）。
- 节点数统计只计算现存且去重节点，修复“幽灵节点 ID”导致的虚高。

### 基础设施

- 自定义编译带 `with_v2ray_api` 的 sing-box-extended 二进制。
- `install.sh` 支持 `--singbox-url` / `KATO_SINGBOX_BINARY_URL`。
- Agent 增加无第三方依赖的 V2Ray API gRPC 客户端。
- 删除节点时自动清理权限组/用户的残留引用；加载时自动清理历史脏数据。

### 清理

- 删除 VLESS / Hysteria2 / Xray 遗留代码、配置与文档。
- 删除设计期 reports、UI QA 截图、废弃 demo/测试脚本、旧阶段文档。

## v0.11.0（2026-08-07）

- AnyTLS 单端口多用户体系落地；按用户统计与限速。

## v0.10.0（2026-08-06）

- 协议收敛为仅 AnyTLS + Realm 中转；删除 VLESS REALITY / Hysteria2。

## v0.9.x（2026-08-05 ~ 08-06）

- 直连访问入口并入协议入站；凭据轮换；前端/订阅服务器资源；AnyTLS 随机二级域名与 Let's Encrypt 自动证书；管理后台路径在线迁移。

## v0.8.x ~ v0.6.x

- 订阅入口缓存/限速/故障兜底；流量统计；监控告警与通知；AnyTLS 支持；主动健康探测。

## v0.5.0 及之前

- 订阅服务器、中文安装向导、自动 TLS、核心资源模型与 Agent 下发体系。
